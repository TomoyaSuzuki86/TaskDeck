AGENTS.md
=========

プロジェクト概要
--------

このリポジトリでは、PWA版のタスク管理アプリ **Task Deck** を開発する。

Task Deck は、通常のToDoリストのように大量のタスクを一覧で管理するアプリではなく、  
「今日やるべきことを、優先順に上から3つだけ見せる」ことを重視したタスクデッキ型アプリである。

ユーザーは、期日・時間・タグ・詳細管理に疲れないように、基本的には音声入力で素早くタスクを追加し、  
カードをスワイプするだけで完了・後回し・保管を行える。

最終的には Google ToDo リストと密に連携し、Googleアカウント上のタスクとTask Deck上のタスクが同期されることを目指す。

* * *

重要な開発方針
-------

### 1. 最初に単体テスト仕様を完成させる

このプロジェクトでは、テスト駆動開発を実践する。

実装より先に、仕様を表す単体テストのTODOを一通り作成すること。

最初の成果物として、以下を満たす状態を作る。

* 主要な仕様がテストケースとして洗い出されている

* テストケース名を読むだけで、アプリの仕様が理解できる

* まだ実装が空でもよい

* `it.todo(...)` や `test.todo(...)` を使って、未実装仕様を明示する

* UI、ドメインロジック、日付処理、Google ToDo連携、認証、同期、PWAの各観点を含める

ユーザーが最初に確認したいのは、この単体テストTODO一覧である。

そのため、最初のステップでは完成実装に進まず、まずテスト仕様の網羅性を優先すること。

* * *

開発完了までの基本フロー
------------

Codexは以下の順番で作業すること。

1. リポジトリ構成を確認する

2. `docs/` 配下の参考画像を確認する

3. AGENTS.mdの内容を理解する

4. アプリ仕様をテストTODOとして作成する

5. テストTODOの一覧をユーザー確認用に整理する

6. ユーザー確認後、実装に進む

7. テストを1つずつ実装済みに変えていく

8. すべての単体テストをグリーンにする

9. PWAとして動作確認する

10. Firebase Hostingへのデプロイを想定した構成に整える

* * *

想定技術スタック
--------

### フロントエンド

* React

* TypeScript

* Vite

* PWA

* CSS Modules または Tailwind CSS

* Vitest

* React Testing Library

### バックエンド・認証・ホスティング

* Firebase

* Firebase Authentication

* Googleログイン

* Firestore

* Firebase Hosting

* 必要に応じて Cloud Functions

### 外部連携

* Google Tasks API

* Googleアカウント連携

* * *

Firebaseプロジェクト
--------------

Firebaseプロジェクトは、事前にユーザーが作成する想定。

想定プロジェクト名は以下。
    Task Deck

実装では、Firebase設定値を直接コードに埋め込まないこと。

`.env.local` などの環境変数を使用すること。

例：
    VITE_FIREBASE_API_KEY=
    VITE_FIREBASE_AUTH_DOMAIN=
    VITE_FIREBASE_PROJECT_ID=
    VITE_FIREBASE_STORAGE_BUCKET=
    VITE_FIREBASE_MESSAGING_SENDER_ID=
    VITE_FIREBASE_APP_ID=

Google Tasks API連携に必要なOAuthスコープや設定も、コード内で明示的に整理すること。

* * *

アプリの基本コンセプト
-----------

### コア体験

Task Deck の中心体験は以下である。
    今日やるべきタスクが、上から3つだけ見える。
    一番上のカードをスワイプすると、次のタスクが上に上がってくる。

通常のタスク管理アプリのように、すべてのタスクを一覧で見せることは主目的ではない。

ユーザーが迷わず、今やるべきことに集中できることを優先する。

* * *

画面イメージ
------

`docs/` フォルダ配下に、直近で作成したTask DeckのUIモック画像を配置する。

Codexは必ずこの画像を参考にすること。

想定配置例：
    docs/task-deck-ui-reference.png

画像から読み取るべき重要ポイント：

* 白を基調としたモダンで柔らかいUI

* 角丸カード

* 淡い影

* 今日やるタスクが上から3つ見える

* 期限切れタスクは最上部に強調表示される

* 画面下部に横長の「タスクを追加」ボタンがある

* 追加ボタン右端に音声入力アイコンがある

* カードはスワイプ操作で完了・後回し・保管できる

* 日付は時刻ではなく日単位で扱う

* Google ToDoリスト連携を想定する

* * *

タスク表示仕様
-------

### 表示対象

メイン画面には、基本的に以下を表示する。

1. 期限切れタスク

2. 今日中のタスク

3. 明日以降の近い日付のタスク

### 表示数

通常タスクは、優先順に最大3件まで表示する。

ただし、期限切れタスクが存在する場合は、期限切れタスクを最優先で表示する。

画面の見え方は以下を基本とする。
    期限切れエリア
    - 期限切れタスク

    今日中エリア
    1. 今日やるタスク
    2. 今日やるタスク
    3. 今日やるタスク

    明日以降エリア
    - 直近の日付のタスクを補助的に表示

### 並び順

タスクの並び順は以下を基本とする。

1. 期限切れタスク

2. 今日中のタスク

3. 明日のタスク

4. それ以降のタスク

5. 期日なしタスク

ただし、音声入力で追加したタスクは、初期状態では「明日」のタスクとして登録する。

* * *

日付仕様
----

### 基本方針

Task Deckでは、時刻ではなく日付だけを扱う。

時刻管理はしない。

### 日付なしの場合

手入力で日付を設定しなかった場合、そのタスクは「今日中」のタスクとして扱う。

### 音声入力の場合

音声入力で追加したタスクは、デフォルトで「明日」のタスクとして登録する。

理由：

* 音声入力は、今思いついたことを素早く逃がす用途

* 今すぐやるタスクに混ざると、現在の集中を妨げる

* 後で手入力画面や編集画面から日付変更できればよい

### 日付ラベル

日付は以下のように表示する。

* 期限切れ

* 今日中

* 明日

* 2日後

* 3日後

* 日付そのもの

* * *

カード操作仕様
-------

### 完了

カードをスワイプして完了する。

完了ボタンはメイン操作として置かない。

完了したカードはリストから消え、次のカードが上に上がる。

### 後回し

後回しは「翌日に回す」操作である。

後回ししたタスクは、期日が翌日に更新される。

ただし、後回し後に同じタスクがすぐ再表示されないようにすること。

#### 後回し後の再表示防止

後回ししたタスクには、一時的な表示抑制状態を持たせる。

例：
    postponedAt: Timestamp
    snoozedUntil: Date

同じ表示セッション内では、後回ししたカードをすぐ再表示しない。

次回の再計算、翌日、または明示的な一覧画面では再表示されてよい。

### 保管

保管は「今のデッキから外す」操作である。

後回しとの違い：

* 後回し：翌日に回す。まだタスクとして残る。

* 保管：今はやらない。通常のデッキ表示から外す。

保管したタスクは、保管リストからいつでも戻せる。

保管時には、期日は変更しない。

ただし、期限切れタスクを保管した場合の扱いは、実装時に以下のどちらかを採用する。

推奨仕様：
    期限切れタスクも保管できる。
    ただし、保管リスト内では期限切れ状態を保持する。
    デッキに戻した場合は、再び期限切れとして最優先に表示される。

* * *

スワイプ操作案
-------

以下を初期案とする。
    左スワイプ：完了
    右スワイプ：後回し
    下スワイプ：保管

ただし、操作ミスが多い場合はUI/UX改善を検討する。

各スワイプには、実行前に視覚的フィードバックを出すこと。

例：

* 完了：緑系のチェック表示

* 後回し：黄色系の翌日表示

* 保管：紫系またはグレー系の保管表示

色だけに依存せず、必ずアイコンとテキストも併用する。

* * *

タスク追加仕様
-------

### 基本UI

メイン画面下部に、横長の追加ボタンを配置する。
    ＋ タスクを追加                     🎙

### 手入力

横長ボタン本体をタップすると、手入力モーダルを開く。

手入力モーダルでは以下を入力できる。

* タスク名

* 日付

* タグ

時刻は入力しない。

### 音声入力

横長ボタン右端のマイクアイコンをタップすると、音声入力モードを開始する。

音声入力では、話した内容をそのままタスク名として登録する。

初期登録仕様：

* 日付：明日

* タグ：なし

音声入力後に、必要であれば編集画面から日付・タグを変更できる。

* * *

タグ仕様
----

### 基本方針

タグは必要。

ただし、音声入力時にはタグを求めない。

### 手入力時

手入力モーダルではタグを設定できる。

タグは複数選択可能とする。

初期候補例：

* 仕事

* 家庭

* 買い物

* 勉強

* 手続き

* その他

ユーザーが新しいタグを追加できるようにしてもよい。

### 音声入力時

音声入力時はタグなしで登録する。

将来的には、よく使う語句からタグを自動提案してもよいが、初期実装では必須ではない。

* * *

Google ToDoリスト連携仕様
------------------

### 基本方針

Googleログイン後、Google Tasks APIを使用してGoogle ToDoリストと連携する。

初期実装では、過剰に複雑な双方向同期は避ける。

ただし、将来的に安定した双方向同期ができるよう、設計は分離しておく。

### 連携対象

* タスク名

* 日付

* 完了状態

* Google側のタスクID

* Google側のリストID

### Google Tasks APIとの違いを吸収すること

Google ToDoリストでは日付・期限の扱いがTask Deckと完全一致しない可能性がある。

Task Deck内部では、日付を日単位で正規化して扱う。

### 同期方針 初期案

初期実装では以下を目指す。

* Task Deckで作成したタスクをGoogle ToDoリストへ作成する

* Task Deckで完了したタスクをGoogle ToDoリスト側でも完了にする

* Google ToDo側のタスクをTask Deckに取り込む

* 同期失敗時はローカル状態を壊さない

### 同期エラー

同期に失敗しても、ユーザーの操作は失わないこと。

Firestoreまたはローカル状態に同期待ち状態を保持する。

* * *

認証仕様
----

### 初回起動

未ログインの場合、Googleログイン画面を表示する。

ログイン後、Task Deckのメイン画面を表示する。

### ログイン目的

Googleログインは以下の目的で使う。

* ユーザー識別

* Firestore上のユーザー別データ管理

* Google ToDoリスト連携

### ログアウト

設定画面からログアウトできるようにする。

ログアウト後はログイン画面へ戻る。

* * *

データモデル案
-------

### Task

    export type TaskStatus = 'active' | 'completed' | 'archived';
    
    export type TaskSource = 'taskDeck' | 'googleTasks';
    
    export type Task = {
      id: string;
      title: string;
      dueDate: string | null; // YYYY-MM-DD
      status: TaskStatus;
      tags: string[];
      order: number;
      source: TaskSource;
      googleTaskId?: string;
      googleTaskListId?: string;
      createdAt: string;
      updatedAt: string;
      completedAt?: string;
      archivedAt?: string;
      postponedAt?: string;
      snoozedUntil?: string;
    };

### Tag

    export type Tag = {
      id: string;
      name: string;
      createdAt: string;
      updatedAt: string;
    };

* * *

単体テストTODOの作成方針
--------------

最初に、以下のようなテストファイルを作ること。
    src/domain/taskDeck/taskOrdering.test.ts
    src/domain/taskDeck/taskActions.test.ts
    src/domain/taskDeck/dateLabel.test.ts
    src/domain/taskDeck/taskCreation.test.ts
    src/domain/googleTasks/googleTaskMapping.test.ts
    src/domain/sync/syncQueue.test.ts
    src/components/TaskDeckHome.test.tsx
    src/components/AddTaskButton.test.tsx
    src/components/ManualTaskModal.test.tsx
    src/components/VoiceTaskInput.test.tsx
    src/auth/authFlow.test.tsx

各ファイルには、まず `test.todo` または `it.todo` で仕様を列挙する。

* * *

必ず含める単体テストTODO
--------------

### タスク並び順

* 期限切れタスクが最優先で表示される

* 今日中のタスクが期限切れの次に表示される

* 明日のタスクが今日中の次に表示される

* 2日後以降のタスクは近い日付順に表示される

* 日付なしタスクは今日中として扱われる

* 完了済みタスクはメインデッキに表示されない

* 保管済みタスクはメインデッキに表示されない

* 通常タスクは最大3件まで表示される

* 今日中のタスクが0件の場合、明日以降の近いタスクが表示される

* 同じ日付のタスクはorder順に表示される

### 日付ラベル

* 昨日以前のタスクは期限切れと表示される

* 今日のタスクは今日中と表示される

* 明日のタスクは明日と表示される

* 2日後のタスクは2日後と表示される

* 3日後のタスクは3日後と表示される

* 一定以上未来の日付はYYYY/MM/DD形式で表示される

* 日付なしタスクは今日中と表示される

### タスク追加

* 手入力でタスク名を指定して追加できる

* 手入力で日付を省略した場合、今日の日付で追加される

* 手入力でタグを設定できる

* 手入力で複数タグを設定できる

* 音声入力でタスクを追加できる

* 音声入力で追加したタスクは明日の日付になる

* 音声入力で追加したタスクにはタグが設定されない

* 空文字のタスクは追加できない

* 前後の空白はトリムされる

### カード操作

* 左スワイプでタスクが完了になる

* 完了したタスクはメインデッキから消える

* 完了後、次のタスクが上に上がる

* 右スワイプでタスクが後回しになる

* 後回ししたタスクの日付は翌日になる

* 後回ししたタスクは同じ表示セッション内ですぐ再表示されない

* 下スワイプでタスクが保管される

* 保管したタスクはメインデッキから消える

* 保管したタスクは保管リストに表示される

* 保管したタスクを復元できる

* 復元した期限切れタスクは期限切れとして表示される

### 期限切れタスク

* 期限切れタスクは最上部に表示される

* 期限切れタスクは後回しできない

* 期限切れタスクは完了できる

* 期限切れタスクは保管できる

* 保管した期限切れタスクはデッキから消える

* 保管リストから戻した期限切れタスクは再び最優先表示される

### 追加ボタンUI

* 横長の追加ボタンが画面下部に表示される

* ボタン本体をタップすると手入力モーダルが開く

* ボタン右端のマイクアイコンをタップすると音声入力が開始される

* マイクアイコンのタップでは手入力モーダルは開かない

* 手入力モーダルではタスク名、日付、タグを入力できる

* 音声入力画面ではタグ入力を求めない

### Googleログイン

* 未ログイン時はログイン画面を表示する

* Googleログイン成功後にメイン画面を表示する

* ログアウトするとログイン画面に戻る

* ログイン中ユーザーのuidを使ってデータを分離する

### Google ToDo連携

* Task DeckのタスクをGoogle Tasks API用の形式に変換できる

* Google Tasks APIのタスクをTask Deck用の形式に変換できる

* Google側の完了状態をTask Deckに反映できる

* Task Deck側の完了状態をGoogle側へ送信できる

* Google側に日付がない場合、Task Deckでは今日中として扱う

* 同期失敗時にローカルタスクが消えない

* 同期待ちタスクを再試行できる

### PWA

* アプリがPWAとしてインストール可能なmanifestを持つ

* オフライン時にも最低限画面が表示される

* オフライン時のタスク追加は同期待ちになる

* オンライン復帰時に同期待ちを処理できる

* * *

UI実装方針
------

### モダンで綺麗な見た目を優先する

Task Deckは、実用性だけでなく、使いたくなる見た目を重視する。

以下を意識する。

* 余白を広めに取る

* 角丸を使う

* 影は薄く自然にする

* 色数を増やしすぎない

* テキストは短くする

* 画面内の情報量を抑える

* スマホ片手操作を意識する

### 画像生成の活用

すべてをHTML/CSSだけで作ろうとしなくてよい。

必要に応じて、コンポーネントに適した画像素材を生成し、画面内に配置してよい。

ただし、以下の条件を守ること。

* 入力フォームやボタンなど、操作が必要なUIはHTML/CSSで実装する

* 背景装飾、空状態、説明イラスト、オンボーディングなどに画像を使う

* 画像がなくても基本操作が成立するようにする

* 画像内に重要な文字を埋め込みすぎない

* アクセシビリティのためにalt属性を設定する

* * *

画面一覧 初期案
--------

### ログイン画面

* アプリ名

* 簡単な説明

* Googleでログインボタン

### メインデッキ画面

* アプリ名

* 期限切れタスクエリア

* 今日中タスクエリア

* 明日以降タスクエリア

* 横長のタスク追加ボタン

* マイク入力ボタン

* 設定ボタン

### 手入力モーダル

* タスク名

* 日付

* タグ

* 保存

* キャンセル

### 音声入力画面

* マイク表示

* 認識中テキスト

* 追加完了フィードバック

* 明日のタスクとして追加される説明

### 保管リスト画面

* 保管済みタスク一覧

* 復元操作

### 設定画面

* Googleアカウント情報

* 同期状態

* ログアウト

* * *

実装時の注意
------

### 過剰実装を避ける

初期バージョンでは、以下は実装しない。

* Pixel Watch対応

* Androidネイティブアプリ

* APK生成

* 複雑なAI自動分類

* 時刻指定

* カレンダー表示

* ガントチャート

* 高度なプロジェクト管理

まずはPWAとして、スマホブラウザで快適に使えることを優先する。

### 将来拡張

PWAとして問題なく動作した後、Androidアプリ化を検討する。

その際は、以下を候補とする。

* Capacitor

* TWA

* React Nativeへの移植

ただし、初期開発では考慮しすぎないこと。

* * *

品質基準
----

### テスト

* ドメインロジックは単体テストを厚めにする

* 日付処理はタイムゾーンの影響を受けないようにする

* UIコンポーネントは主要操作をテストする

* Google Tasks APIは直接叩かず、モック可能なアダプタ層を作る

### 型安全

* TypeScriptの型を活用する

* `any` を安易に使わない

* API境界では型変換を明示する

### 保守性

* UIとドメインロジックを分離する

* Google Tasks API依存をアプリ全体に広げない

* Firebase依存もRepository層に閉じ込める

* * *

Codexへの最初の依頼内容
--------------

最初に実施すること。
    このAGENTS.mdを読み、Task Deckの仕様を満たす単体テストTODOを一通り作成してください。
    まだ実装は進めなくてよいです。
    ユーザーが確認できるように、作成したテストTODOの一覧と、どの仕様をカバーしているかを整理してください。

* * *

完成の定義
-----

初期完成の定義は以下。

* Googleログインできる

* メイン画面に今日やるタスクが最大3件表示される

* 期限切れタスクが最優先で表示される

* タスクを手入力で追加できる

* タスクを音声入力で追加できる

* 音声入力タスクは明日のタスクとして登録される

* カードスワイプで完了・後回し・保管ができる

* 後回しタスクがすぐ再表示されない

* 保管リストから復元できる

* Google ToDoリストと基本同期できる

* PWAとしてスマホで使える

* 主要な単体テストがすべてグリーンになっている
